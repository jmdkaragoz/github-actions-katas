name: hello-world
on: push
jobs:
  my-job:
    runs-on: ubuntu-latest
    container: gradle:6-jdk11
    steps:
      - uses: actions/checkout@2
      - name: my-step
        run: echo "Hello World!"
      - name: build
        run: ci/build-app.sh
      - name: Test
        run: ci/test-app.sh